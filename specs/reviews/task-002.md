# Review: Task 002

## Round 1

- [process-revision-complete] `_pascal_to_kebab` fails for entity types with consecutive uppercase letters (acronyms). `ontology.py:295-302` uses `re.sub(r"([a-z0-9])([A-Z])", ...)` which only inserts hyphens between a lowercase/digit and an uppercase letter. For "SREFile" (referenced in spec section 1.5 as an example entity type), `_pascal_to_kebab("SREFile")` returns `"srefile"` instead of `"sre-file"`. This breaks `find_entity_type_for_slug` and `get_entities_by_type` for any entity type containing acronyms. No test covers this case.
- [process-revision-complete] Missing `entities` dict key-to-slug consistency validator on the `Ontology` model. `ontology.py:125-146` has model validators ensuring `entity_types` dict keys match their `type` field and `relationship_types` dict keys match their `composite_key`, but there is no corresponding validator for the `entities` dict (line 122). Constructing `Ontology(entities={"wrong-key": EntityInstance(slug="product:test", properties={})})` silently succeeds, after which `get_entity_by_slug("product:test")` returns `None` despite the entity existing in the dict. Spec section 2.2 requires slugs to be the primary identifier; the inconsistency between key and slug breaks all slug-based lookups.
- [process-revision-complete] No enforcement of relationship instance uniqueness. Spec section 3.2 states: "The `(source_slug, target_slug)` pair is unique within a composite key." `Ontology.relationships` is a plain `list[RelationshipInstance]` (line 123) with no uniqueness constraint. Neither the container nor `validate_relationship` (lines 237-276) checks for duplicate `(composite_key, source_slug, target_slug)` triples, so the same relationship can be inserted multiple times without error.
- [process-revision-complete] `validate_entity` does not enforce structural type protection. Spec section 4.4 states: "Validation rejects edits to protected types." The `Ontology` model provides query helpers (`EntityTypeDefinition.is_structural` at line 65, `Ontology.is_structural_entity_type` at line 278), but `validate_entity` (lines 186-235) never consults them. An entity belonging to a structural-tier type (e.g., `DataSource` with `tier=structural`) passes `validate_entity` without any protection error. The task acceptance criteria explicitly lists "Structural type protection (tier-based editability check)."

## Round 2

- [process-revision-complete] `validate_entity` rejects properties not in `required_properties` or `optional_properties` (`ontology.py:246-249`), but spec section 4.1 lists exactly five entity validation rules — required properties, property type validation, tag validation, slug presence, slug global uniqueness — and "unknown property rejection" is not among them. The spec's rule 4.1.2 ("Values must match expected types") governs value types, not which property names are permitted. This makes entity validation stricter than the spec requires, potentially rejecting valid entities that carry extra metadata properties. Additionally, `validate_relationship` does NOT perform the corresponding check against `required_parameters`/`optional_parameters` — a relationship with `properties={"since": "2024", "extra_key": "val"}` passes validation even if `extra_key` is not in the type definition's parameter lists. The asymmetry means entity and relationship validation enforce different rules about undeclared properties without spec basis for either behavior.
- [process-revision-complete] `RelationshipInstance.properties` is typed as `dict[str, PropertyValue]` where `PropertyValue = str | bool | int | list[str]` (imported from `entities.py` at `relationships.py:13`), but the spec only defines these property type restrictions for entity properties in section 2.3 (which falls under "2. Entity Model"). Section 3.1 describes relationship properties as `object` — a generic "Key-value map; may be empty" — with no type constraints on values. By reusing `PropertyValue`, the implementation prevents relationship properties from containing types like `float` or nested objects that the spec does not prohibit. For example, constructing `RelationshipInstance(..., properties={"weight": 0.75})` raises a Pydantic `ValidationError`, yet the spec imposes no such restriction on relationship property values.

## Round 3

- [process-revision-complete] `_pascal_to_kebab` is not injective — different PascalCase entity type names can produce the same kebab-case slug prefix, and the `Ontology` model does not validate against this. Verified: `_pascal_to_kebab("SREFile")` and `_pascal_to_kebab("SreFile")` both return `"sre-file"`; same for `"APIClient"`/`"ApiClient"` → `"api-client"`. An `Ontology` with both `SREFile` and `SreFile` in `entity_types` is accepted without error (`ontology.py:125-147` validates dict keys match type names, but does not check for kebab-prefix collisions). When prefixes collide, `find_entity_type_for_slug` (`ontology.py:198-204`) returns whichever entity type definition it encounters first in dict iteration order, and `get_entities_by_type` (`ontology.py:189-196`) returns entities belonging to either type indiscriminately. This breaks spec section 2.2's slug global uniqueness requirement ("Globally unique — a slug cannot appear in more than one entity type") and section 4.1 rule 5 ("Slug global uniqueness"), because entities of two distinct types share the same slug prefix format and cannot be distinguished.
- [process-revision-complete] Tag validation in `validate_entity` (`ontology.py:250-260`) silently accepts a `tags` property that is not a list. The guard `isinstance(tags, list)` at line 252 skips all tag validation when `tags` is a non-list `PropertyValue` (e.g., a string). Verified: constructing `EntityInstance(slug="product:test", properties={"name": "Test", "tags": "core"})` and calling `ontology.validate_entity(entity)` returns zero errors, even when the entity type defines `tag_definitions={"core": "Core"}`. Spec section 2.4 states "Tags are an array of strings" — a scalar `tags` value violates this requirement but is never caught. The validation should report an error when `tags` is present but is not a list, rather than silently skipping the tag membership check.
